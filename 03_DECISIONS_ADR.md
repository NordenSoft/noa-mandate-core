# NOA Receipt Decision Index

This index routes decisions; it does not replace the cited source or its exact revision. A proposed document is not an accepted requirement. `CORRECTIONS.md` records the maintained public claim corrections.

| ID | Status | Decision | Authoritative source |
| --- | --- | --- | --- |
| ADR-R-001 | ACCEPTED / frozen scope | `noa.receipt/0.1` has stable wire semantics and must not gain convenience fields through adapters. | [docs/draft-noa-receipt-00.md](docs/draft-noa-receipt-00.md), [VERSIONING.md](VERSIONING.md) |
| ADR-R-002 | ACCEPTED | Native JSON verification, package semver, and optional COSE carriage/algorithm identifiers are separate compatibility axes. | [VERSIONING.md](VERSIONING.md), [docs/draft-noa-receipt-00.md](docs/draft-noa-receipt-00.md) |
| ADR-R-003 | ACCEPTED | Receipt signatures prove origin and integrity of a statement, not truth, current authorization, physical completion, completeness, exactly-once execution, or human understanding. | [NON-CLAIMS.md](NON-CLAIMS.md) |
| ADR-R-004 | ACCEPTED / field semantic preservation | `action.id` is the action/tool identifier; `action.canonical` is the risk-table key; `paramsHash` is not a universal re-derived digest; absent `0.1` fields are `SOURCE_ABSENT`; `PARTIAL` cannot silently become `PASS`. | [docs/draft-noa-receipt-00.md](docs/draft-noa-receipt-00.md), [NON-CLAIMS.md](NON-CLAIMS.md) |
| ADR-R-005 | PROPOSED | Trusted-input provenance must bind each trust input to its evidence origin and fail closed when provenance is absent or invalid. | [THREAT-MODEL.md](THREAT-MODEL.md), [05_RISK_REGISTER.md](05_RISK_REGISTER.md) |
| ADR-R-006 | UNRESOLVED | Define an additive, versioned integration contract that preserves receipt field semantics and detects consumer mismatch. | [04_ROADMAP.md](04_ROADMAP.md) |
| ADR-R-007 | UNRESOLVED | Define a public conformance profile and independence criteria before claiming two independent implementations. | [conformance/MATRIX.md](conformance/MATRIX.md), [04_ROADMAP.md](04_ROADMAP.md) |
| ADR-R-008 | PARTIALLY RESOLVED | Signer roles, `kid` binding, and layered native-chain verification are implemented and covered by conformance vectors. COSE protected-header placement, embedded-signature presence, exact payload bytes, and detached-payload binding still require revision-specific evidence before an interoperability claim. | [docs/receipt-spec.md](docs/receipt-spec.md), [docs/ietf/draft-noa-scitt-ai-agent-receipt.md](docs/ietf/draft-noa-scitt-ai-agent-receipt.md), `src/cose/receipt-cose.ts`, `src/cose/cose-sign1.ts` |
| ADR-R-009 | ACCEPTED | `noa.deploy.release/1` is the public wire form of the deployment action bind: six required named members (`repository`, `commit`, `environment`, `imageDigest`, `targetAccount`, `operation`), closed-world, JCS-canonicalized to `paramsHash` with no domain tag, plus a normative display projection and two pinned projection identities. It is additive and disjoint from `noa.receipt/0.1`, which gains no field (ADR-R-001). Consistent with ADR-R-004, it does not make `paramsHash` a universal cross-producer digest; it publishes the rule for one declared `action.canonical`. The pinned hashes are normative expected values, not attestations about any running system (`NON-CLAIMS.md` §S6). | [docs/deploy-release-spec.md](docs/deploy-release-spec.md), `src/deploy-release.ts`, [conformance/deploy-release/vectors.json](conformance/deploy-release/vectors.json) |
| ADR-R-010 | PROPOSED | `noa.ledger.transfer/1` is the public wire form of a reference ledger transfer: six required string members (`amount`, `fromAccount`, `ledger`, `salt`, `toAccount`, `unit`), closed-world, one spelling per value, `fromAccount` distinct from `toAccount`, JCS-canonicalized to `paramsHash` with no domain tag, a complete and invertible six-row display, eleven stable per-member refusal codes in a normative order, and two pinned projection identities whose implementation digest is recomputable from the published package. A 128-bit `salt` member keeps the tuple from being confirmed from `paramsHash` alone. The unit is the closed enum {`XTS`}, scale 0. It is additive and disjoint from `noa.receipt/0.1` (ADR-R-001) and, consistent with ADR-R-004, publishes the rule for one declared `action.canonical` only. No JSON Schema ships with `/1`: the normative validator is the specification plus the corpus, and the repository carries no schema validator. The reference Gate registers the adapter only as an effect-owned action (ADR-R-012). Status moves to ACCEPTED only after independent review; the pinned hashes are normative expected values, not attestations (`NON-CLAIMS.md` §S7). | [docs/ledger-transfer-spec.md](docs/ledger-transfer-spec.md), `src/ledger-transfer.ts`, [conformance/ledger-transfer/vectors.json](conformance/ledger-transfer/vectors.json) |
| ADR-R-011 | PROPOSED | The reference gate gains a PINNED trust mode: a persistent gate key loaded from a file it never creates, and approver, audit, epoch and quorum taken only from an operator-provisioned roster (`noa.gate-roster/1`). The roster is authenticated in `/1` by file discipline (absolute path with every symlink vetted, owner other than the gate's uid, mode, link count, ancestor walk; a gate running as root is refused), a printed digest and an optional digest pin; a high-water state file, read and written under a lock, refuses rollback. Decisions are checked against the gate's audience, the epoch and the sealed display's recipients in both modes; roster expiry, class and quorum in pinned mode. No fallback to the self-minted alpha root exists, and alpha keeps no display sealer. Reference configuration only: it is not wire language and carries no conformance claim (`NON-CLAIMS.md` §S8). | [docs/gate-pinned-trust.md](docs/gate-pinned-trust.md), `packages/gate/src/roster.ts`, `packages/gate/src/trust.ts` |
| ADR-R-012 | PROPOSED | The reference gate commits an EFFECT-OWNED action itself: `noa.ledger.transfer` is registered together with a static effect-owned table, and `POST /v1/holds/:holdId/commit` (owner-scoped, no body) hands the stored artifacts to an in-process effect owner. The owner defends against a hostile caller of its commit API (hostile input objects, a hostile sealer): it reads its input once, refuses re-entry for the whole commit, and, against the pinned trust root, re-checks the hold's boot (the stored identifier and the signed freeze time) and re-verifies the hold envelope (signature, tenant, gate, hold id, epoch, mode), the deferred and approval receipts (signatures, linkage, ALLOWED verdict, the held action), the execution grant and its bindings, the approver decision (at the grant's signed `issuedAt`), that the approval receipt is the deciding approver's under the decision keyring's key and that the bound display was sealed to that approver; it enforces uniqueness over the envelope, decision and grant keys (again just before the write), re-derives the params hash from the snapshot's canonical text, checks ledger, accounts and funds, signs the EXECUTED receipt and consumption from its own verified parses, verifies what it signed and that it is bound to this commit's instant, and writes one row whose write consumes the authority; a ledger refusal is an unsigned, terminal REFUSED row. One effect-owning engine serves a boot in a process. What the owner cannot check from its artifacts is listed in `NON-CLAIMS.md` §S9. The grant is withheld from every hold view until an EXECUTED row exists, and `reserve`/`report` refuse such actions. A hold records its boot identifier: `decide` and `commit` refuse a hold frozen by another boot. The owner exists only over a pinned trust root. The hold resolution keeps `HUMAN_APPROVED_INTENT_NOT_EXECUTION_BOUND`. Reference implementation only: the ledger is in memory, and the route and codes are not wire language (`NON-CLAIMS.md` §S9). | [docs/gate-effect-owner.md](docs/gate-effect-owner.md), `packages/gate/src/effect-owner.ts`, `packages/gate/src/engine.ts` |
| ADR-R-013 | PROPOSED | The reference gate exposes the hooks a durable effect owner builds on, derived from the repaired owner. `verifyEffectAuthority` (the entry snapshot, the hold's boot, O1 and the signed boot), the pure `deriveLedgerCommit` (O3 to O5) and the pure `verifyEffectAttestation` (O8) are exported and are what the in-memory owner itself calls, so another owner runs one implementation; the keyring is consumed at one verification context, and the re-entry guard and the owner's record stay the owner's. An owner may record the grant's consumption itself (`recordsGrantConsumption` with its `store`, which must be the engine's: `EFFECT_OWNER_STORE_MISMATCH`), and `EFFECT_STORE_UNAVAILABLE` is a retryable 503 that wrote nothing; an owner that cannot tell whether its write committed answers `OUTCOME_UNKNOWN` instead, which the engine answers `503 EFFECT_OUTCOME_UNKNOWN` (retryable only as a commit of the same hold, nothing mirrored; `conformance/gate-commit-answers/vectors.json` pins every answer without a row). Every transition out of PENDING (decide, expiry by read, long-poll or sweep, cancel) goes through `Store.settleHold`, a compare-and-swap on the PENDING status that records an approval's grant in the same step; a loser persists and returns nothing it signed. `createHold`'s 201 and every hold view carry the deferred receipt. A supervisor may supply the pinned `bootId` (32 lowercase hex, else `BOOT_ID_INVALID`) together with the boot's start `bootStartedAt` (canonical UTC, at most five seconds ahead, else `BOOT_START_INVALID`); decide and the owner both refuse a hold whose gate-signed freeze time precedes the boot's start (`frozenBeforeBoot`), and under a supervisor `bootId` the engine accepts only an owner that records the consumption in its store (`EFFECT_OWNER_SUPERVISOR_BOOT_UNSAFE`); the banner reports both sources. `noa.gate-pin/1` defines the Gate-pin fingerprint a person compares before pinning a Gate on a device (display only; `gatePin` in the banner and `roster-check`), with a conformance corpus. An owner conformance runner holds another owner to the reference owner's owner-level tests (the keyring proof body exported beside it), and a store contract holds another store to `settleHold`'s semantics. The older trust setup's keyrings are null-prototype. Reference implementation only (`NON-CLAIMS.md` NC-S9.12 to NC-S9.16). | [docs/gate-effect-owner.md](docs/gate-effect-owner.md), [docs/gate-pin-spec.md](docs/gate-pin-spec.md), `packages/gate/src/effect-owner.ts`, `packages/gate/src/store.ts`, `packages/gate/src/engine.ts`, `packages/gate/src/gate-pin.ts` |
| ADR-0003 | RECORDED DIRECTION / PROPOSED ARCHITECTURE | Records an enforcement-boundary direction while retaining the detailed architecture as proposed; it establishes no implementation, deployment, or release claim. | [docs/ADR-0003-enforcement-boundary.md](docs/ADR-0003-enforcement-boundary.md) |

Future ADRs must name the problem, alternatives, accountable decision maker, exact normative revision, compatibility effect, security/privacy impact, required vectors, implementation work, and rollback/forward-fix path.
