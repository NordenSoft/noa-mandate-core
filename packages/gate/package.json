{
  "name": "noa-gate",
  "version": "0.1.0",
  "private": true,
  "description": "NOA generic HTTP gate + exact-execution wrapper (spec §8, the THIRD door). The TRUSTED signer half of the approval protocol: unlike the relay (untrusted transport, never signs), the gate SIGNS the Hold Envelope (D1), the pre-execution Execution Grant (D13), the post-execution Consumption, the gate-determined Execution Uncertainty (F8c), the gate-signed Hold Resolution (F10) and the POLICY-signed timeout receipt (D19). It owns the authoritative single-use grant record, whose UNUSED->RESERVED compare-and-set is atomic (`packages/gate/src/engine.ts`, reserve-before-dispatch, F8a) and the exact-execution binding (D3/D14/D18): a params-hash mismatch REFUSES to run. A thin node:http shim over pure primitives, loopback-by-default (D20).",
  "keywords": [
    "ai-agent",
    "governance",
    "approval",
    "human-in-the-loop",
    "execution-grant",
    "exact-execution"
  ],
  "homepage": "https://noatrust.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/NordenSoft/noa.git",
    "directory": "packages/gate"
  },
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "bin": {
    "noa-gate": "dist/src/cli.js",
    "noa-gate-grant-signer": "dist/src/grant-sidecar.js"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist/src",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "npm run build && node dist/src/cli.js"
  },
  "dependencies": {
    "noa-approval-artifacts": "file:../approval-artifacts",
    "noa-receipt": "file:../..",
    "noa-mcp-adapter-core": "file:../adapter-core"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
